{
  "identifier": "default",
  "description": "Default capability for file analysis",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:allow-read-file",
    "dialog:allow-open"
  ],
  "scope": {
    "fs": {
      "allow": [{ "path": "**" }]
    }
  }
}
